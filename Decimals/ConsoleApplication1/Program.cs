using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ConsoleApplication1
{
    class Program
    {
        static void Main(string[] args)
        {
            int length = 15;
            int width = 25;
            int area = (length * width);
            Console.WriteLine("If the length is {0} and the width is {1}, then the area is {2}.", length,width,area);
        }
    }
}
